# userver: Shared Functionality

Files that are shared by `userver-core` and `userver-universal`. In other words - all
the functions and types that do not depend on asynchronous engine and logging
should go here.
