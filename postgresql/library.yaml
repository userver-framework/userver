project-name: yandex-taxi-userver-postgresql
maintainers:
  - Common components

libraries:
  - Boost::regex
  - ICU::i18n
  - ICU::uc
  - PostgreSQL
  - yandex-taxi-userver-core

debian:
  build_dependencies:
    - libboost-regex-dev
    - libicu-dev
    - libpq-dev
    - postgresql-server-dev-10

configs:
  names:
  - POSTGRES_DEFAULT_COMMAND_CONTROL
