type: object
description: process-starter
additionalProperties: false
properties:
    task_processor:
        type: string
        description: |
            The name of the TaskProcessor for process starting.

            Default: the 'default_task_processor' value from components::ManagerControllerComponent
