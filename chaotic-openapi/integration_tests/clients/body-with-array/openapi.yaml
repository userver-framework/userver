openapi: 3.0.0
info:
    title: test-object client
    version: '1.0'

paths:
    /test1:
        post:
            requestBody:
                content:
                    application/json:
                        schema:
                            type: array
                            items:
                                type: string
            responses:
                '200':
                    description: OK
