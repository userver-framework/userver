#!/bin/sh

set -e

if [ "$1" = "configure" ]; then
	if ! dpkg-statoverride --list /var/log/yandex/taxi-driver-authorizer >/dev/null 2>&1; then
		dpkg-statoverride --update --add www-data www-data 0755 /var/log/yandex/taxi-driver-authorizer
	fi
fi

#DEBHELPER#

exit 0
