project-name: userver-mongo
project-alt-names:
  - yandex-userver-mongo
maintainers:
  - Common components
description: MongoDB serialization helpers

libraries:
  - userver-core

configs:
    names:
      - MONGO_DEADLINE_PROPAGATION_ENABLED
      - MONGO_DEFAULT_MAX_TIME_MS
