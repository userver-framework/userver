# AUTOGENERATED, DON'T CHANGE THIS FILE!

 
                  
 
 

include(FindPackageHandleStandardArgs)

find_library(Fmt_LIBRARIES_fmt NAMES
  fmt
  )
list(APPEND Fmt_LIBRARIES
  ${Fmt_LIBRARIES_fmt}
)
  
find_path(Fmt_INCLUDE_DIRS_fmt_format_h NAMES
  fmt/format.h
  )
list(APPEND Fmt_INCLUDE_DIRS
  ${Fmt_INCLUDE_DIRS_fmt_format_h}
)
  
 

if(UNIX AND NOT APPLE)
  find_program(DPKG_QUERY_BIN dpkg-query)
  if(DPKG_QUERY_BIN)
    execute_process(
      COMMAND dpkg-query --showformat=\${Version} --show libfmt-dev
      OUTPUT_VARIABLE Fmt_version_output
      ERROR_VARIABLE Fmt_version_error
      RESULT_VARIABLE Fmt_version_result
      OUTPUT_STRIP_TRAILING_WHITESPACE
    )
    if(Fmt_version_result EQUAL 0)
      set(Fmt_VERSION ${Fmt_version_output})
      message(STATUS "Installed version libfmt-dev: ${Fmt_VERSION}")
    endif(Fmt_version_result EQUAL 0)
  endif(DPKG_QUERY_BIN)
endif(UNIX AND NOT APPLE)
 
if(APPLE)
  find_program(BREW_BIN brew)
  if(BREW_BIN)
    execute_process(
      COMMAND brew list --versions fmt
      OUTPUT_VARIABLE Fmt_version_output
      ERROR_VARIABLE Fmt_version_error
      RESULT_VARIABLE Fmt_version_result
      OUTPUT_STRIP_TRAILING_WHITESPACE
    )
    if(Fmt_version_result EQUAL 0)
      if (Fmt_version_output MATCHES "^(.*) (.*)$")
        set(Fmt_VERSION ${CMAKE_MATCH_2})
        message(STATUS "Installed version fmt: ${Fmt_VERSION}")
      else()
        set(Fmt_VERSION "NOT_FOUND")
      endif()
    else()
      message(WARNING "Failed execute brew: ${Fmt_version_error}")
    endif(Fmt_version_result EQUAL 0)
  endif(BREW_BIN)
endif(APPLE)
 
 
 
find_package_handle_standard_args(
  Fmt
    REQUIRED_VARS
      Fmt_LIBRARIES
      Fmt_INCLUDE_DIRS
      
    FAIL_MESSAGE
      "Could not find `Fmt` package. Debian: sudo apt update && sudo apt install libfmt-dev MacOS: brew install fmt Version: 7.1"
)
mark_as_advanced(
  Fmt_LIBRARIES
  Fmt_INCLUDE_DIRS
  
)
 
if(NOT Fmt_FOUND)
  message(FATAL_ERROR "Could not find `Fmt` package. Debian: sudo apt update && sudo apt install libfmt-dev MacOS: brew install fmt Version: 7.1")
elseif(Fmt_version_result)
  if(Fmt_version_result EQUAL 0)
    if(Fmt_VERSION VERSION_LESS 7.1)
      message(FATAL_ERROR "Found but version is Fmt_VERSION. Could not find `Fmt` package. Debian: sudo apt update && sudo apt install libfmt-dev MacOS: brew install fmt Version: 7.1")
    endif()
  else()
    message(WARNING "Can not determine version. ${Fmt_version_error}")
  endif()
endif()

 
if (NOT TARGET Fmt)
  add_library(Fmt INTERFACE IMPORTED GLOBAL)
   target_include_directories(Fmt INTERFACE ${Fmt_INCLUDE_DIRS})
   target_link_libraries(Fmt INTERFACE ${Fmt_LIBRARIES})
  endif(NOT TARGET Fmt)
