# AUTOGENERATED, DON'T CHANGE THIS FILE!


if (TARGET locale)
  if (NOT locale_FIND_VERSION)
      set(locale_FOUND ON)
      return()
  endif()

  if (locale_VERSION)
      if (locale_FIND_VERSION VERSION_LESS_EQUAL locale_VERSION)
          set(locale_FOUND ON)
          return()
      else()
          message(FATAL_ERROR
              "Already using version ${locale_VERSION} "
              "of locale when version ${locale_FIND_VERSION} "
              "was requested."
          )
      endif()
  endif()
endif()

set(FULL_ERROR_MESSAGE "Could not find `locale` package.\n\tDebian: sudo apt update && sudo apt install libboost-locale-dev\n\tMacOS: brew install boost")


include(FindPackageHandleStandardArgs)

find_library(locale_LIBRARIES_boost_locale
  NAMES boost_locale
)
list(APPEND locale_LIBRARIES ${locale_LIBRARIES_boost_locale})

find_path(locale_INCLUDE_DIRS_boost_locale_config_hpp
  NAMES boost/locale/config.hpp
)
list(APPEND locale_INCLUDE_DIRS ${locale_INCLUDE_DIRS_boost_locale_config_hpp})



if (locale_VERSION)
  set(locale_VERSION ${locale_VERSION})
endif()

if (locale_FIND_VERSION AND NOT locale_VERSION)
  include(DetectVersion)

  if (UNIX AND NOT APPLE)
    deb_version(locale_VERSION libboost-locale-dev)
  endif()
  if (APPLE)
    brew_version(locale_VERSION boost)
  endif()
endif()

 
find_package_handle_standard_args(
  locale
    REQUIRED_VARS
      locale_LIBRARIES
      locale_INCLUDE_DIRS
      
    FAIL_MESSAGE
      "${FULL_ERROR_MESSAGE}"
)
mark_as_advanced(
  locale_LIBRARIES
  locale_INCLUDE_DIRS
  
)

if (NOT locale_FOUND)
  if (locale_FIND_REQUIRED)
      message(FATAL_ERROR "${FULL_ERROR_MESSAGE}. Required version is at least ${locale_FIND_VERSION}")
  endif()

  return()
endif()

if (locale_FIND_VERSION)
  if (locale_VERSION VERSION_LESS locale_FIND_VERSION)
      message(STATUS
          "Version of locale is '${locale_VERSION}'. "
          "Required version is at least '${locale_FIND_VERSION}'. "
          "Ignoring found locale."
      )
      set(locale_FOUND OFF)
      return()
  endif()
endif()

 
if (NOT TARGET locale)
  add_library(locale INTERFACE IMPORTED GLOBAL)

  if (TARGET Boost::locale)
    target_link_libraries(locale INTERFACE Boost::locale)
  endif()
  target_include_directories(locale INTERFACE ${locale_INCLUDE_DIRS})
  target_link_libraries(locale INTERFACE ${locale_LIBRARIES})
  
  # Target locale is created
endif()

if (locale_VERSION)
  set(locale_VERSION "${locale_VERSION}" CACHE STRING "Version of the locale")
endif()
