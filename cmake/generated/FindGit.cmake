# AUTOGENERATED, DON'T CHANGE THIS FILE!

 
      
 
 

include(FindPackageHandleStandardArgs)

 
 
find_program(Git_EXECUTABLE_git_eg NAMES
  git eg
    PATH_SUFFIXES
      Git/cmd Git/bin
  )
list(APPEND Git_EXECUTABLE
  ${Git_EXECUTABLE_git_eg}
)
  

 
 
find_package_handle_standard_args(
  Git
    REQUIRED_VARS
      
      
      Git_EXECUTABLE
    FAIL_MESSAGE
      "Could not find `Git` package."
)
mark_as_advanced(
  
  
  Git_EXECUTABLE
)
 
if(NOT Git_FOUND)
  message(FATAL_ERROR "Could not find `Git` package.")
elseif(Git_version_result)
  if(Git_version_result EQUAL 0)
    if(Git_VERSION VERSION_LESS None)
      message(FATAL_ERROR "Found but version is Git_VERSION. Could not find `Git` package.")
    endif()
  else()
    message(WARNING "Can not determine version. ${Git_version_error}")
  endif()
endif()

 
