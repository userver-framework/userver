project-name: yandex-taxi-userver-redis
maintainers:
  - Common components

link-targets:
  - Hiredis
  - yandex-taxi-userver-core

debian:
  build_dependencies:
    - libhiredis-dev

configs:
  names:
  - REDIS_DEFAULT_COMMAND_CONTROL
  - REDIS_SUBSCRIBER_DEFAULT_COMMAND_CONTROL
  - REDIS_SUBSCRIPTIONS_REBALANCE_MIN_INTERVAL_SECONDS
