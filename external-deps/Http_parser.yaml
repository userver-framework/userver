name: Http_Parser
version: "2.8"
helper-prefix: false

includes:
  names:
    - http_parser.h

libraries:
  names:
    - http_parser
    - libhttp_parser

debian-names:
  - libhttp-parser-dev
formula-name: http-parser
