name: Http_Parser
version: "2.1"
helper-prefix: false

includes:
    find:
      - names:
          - http_parser.h

libraries:
    find:
      - names:
          - http_parser
          - libhttp_parser

debian-names:
  - libhttp-parser-dev
formula-name: http-parser
