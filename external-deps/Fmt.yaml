name: Fmt
version: '7.1'  # on update also adjust the version in core/CMakeLists.txt
helper-prefix: false

includes:
    find:
      - names:
          - fmt/format.h

libraries:
    find:
      - names:
          - fmt

debian-names:
  - libfmt-dev
formula-name: fmt
