common-name: OpenSSL
partials:
  - name: Crypto
    package-name: OpenSSL
    helper-prefix: false

    debian-names:
      - libssl-dev
    formula-name: openssl
    rpm-names:
      - openssl-devel

    libraries:
        enabled: false

    includes:
        enabled: false
  - name: SSL
    package-name: OpenSSL
    helper-prefix: false

    debian-names:
      - libssl-dev
    formula-name: openssl
    rpm-names:
      - openssl-devel

    libraries:
        enabled: false

    includes:
        enabled: false
