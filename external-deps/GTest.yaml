name: gtest
type: external-project

source:
    dir: submodules/googletest

build-args:
    names:
      - BUILD_GTEST=ON
      - BUILD_GMOCK=OFF

targets:
  - name: libgtest
    libs:
      - googletest/libgtest.a
      - googletest/libgtest_main.a
    includes:
      - googletest/include
