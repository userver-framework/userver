name: spdlog
version: '1.6.0'  # on update also adjust the version in cmake/SetupSpdlog.cmake

includes:
    find:
      - names:
          - spdlog/logger.h

libraries:
    find:
      - names:
          - spdlog

debian-names:
  - libspdlog-dev
formula-name: spdlog
rpm-names:
  - spdlog-devel
pacman-names:
  - spdlog
pkg-config-names:
  - spdlog
